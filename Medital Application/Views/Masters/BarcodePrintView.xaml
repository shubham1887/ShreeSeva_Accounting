<UserControl x:Class="Medital_Application.Views.Masters.BarcodePrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="#F5F7FA">

    <UserControl.Resources>
        <!-- Card border style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="1" Opacity="0.6"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary button style -->
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#90CAF9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button style -->
        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="Foreground" Value="#1565C0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#90CAF9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox style -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid row style -->
        <Style x:Key="GridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid header style -->
        <Style x:Key="GridHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Top Filter Bar ─────────────────────────────────────────── -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="12,12,12,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Style="{StaticResource SearchBox}"
                             Width="320"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Search products by name or barcode…">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Content="Search" Margin="8,0,0,0"
                            Style="{StaticResource SecondaryBtn}"
                            Command="{Binding SearchCommand}"/>
                </StackPanel>

                <!-- Label size selector -->
                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Label Size" FontSize="11" Foreground="#546E7A" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{x:Static local:BarcodePrintView.LabelSizes}"
                              SelectedItem="{Binding SelectedLabelSize}"
                              Padding="8,6" FontSize="12" BorderBrush="#BDBDBD"
                              xmlns:local="clr-namespace:Medital_Application.Views.Masters"/>
                </StackPanel>

                <!-- Loading indicator -->
                <TextBlock Grid.Column="2" Text="Loading…"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                           Foreground="#1565C0" FontSize="12" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- ── Main Split Panel ──────────────────────────────────────── -->
        <Grid Grid.Row="1" Margin="12,0,12,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <!-- Product Grid -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Products" FontSize="14"
                               FontWeight="Bold" Foreground="#1565C0" Margin="0,0,0,10"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Products}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#F0F0F0"
                              BorderThickness="0"
                              RowStyle="{StaticResource GridRow}"
                              ColumnHeaderStyle="{StaticResource GridHeader}"
                              Background="White"
                              AlternatingRowBackground="#FAFAFA">

                        <DataGrid.Columns>
                            <!-- Checkbox -->
                            <DataGridTemplateColumn Width="40" Header="✓">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Product Name"
                                               Binding="{Binding ProductName}"
                                               Width="*" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="6,4"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Barcode"
                                               Binding="{Binding Barcode}"
                                               Width="130" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="6,4"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontFamily" Value="Consolas"/>
                                        <Setter Property="FontSize" Value="11"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="MRP"
                                               Binding="{Binding Mrp, StringFormat=N2}"
                                               Width="80" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="6,4"/>
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Sale Rate"
                                               Binding="{Binding SaleRate, StringFormat=N2}"
                                               Width="80" IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Padding" Value="6,4"/>
                                        <Setter Property="TextAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Copies — editable -->
                            <DataGridTemplateColumn Header="Copies" Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Copies}" Padding="6,4"
                                                   TextAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Copies, UpdateSourceTrigger=PropertyChanged}"
                                                 Padding="4" TextAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center"
                          Background="#E0E0E0"/>

            <!-- Right: Label Preview Panel -->
            <Border Grid.Column="2" Style="{StaticResource Card}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Label Preview" FontSize="14"
                               FontWeight="Bold" Foreground="#1565C0" Margin="0,0,0,14"/>

                    <!-- Simulated label card -->
                    <Border BorderBrush="#BDBDBD" BorderThickness="1" CornerRadius="4"
                            Padding="10" Background="#FAFAFA" MinHeight="100">
                        <StackPanel>
                            <!-- Barcode placeholder image area -->
                            <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                                    Height="60" Margin="0,0,0,6" HorizontalAlignment="Stretch">
                                <TextBlock Text="[Barcode Preview]"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           Foreground="#9E9E9E" FontSize="11" FontStyle="Italic"/>
                            </Border>
                            <TextBlock Text="Product Name" FontSize="11"
                                       Foreground="#37474F" FontWeight="SemiBold"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="MRP: ₹ 0.00  |  Batch: ——"
                                       FontSize="10" Foreground="#546E7A"
                                       HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Select a product to preview its label"
                               FontSize="11" Foreground="#9E9E9E"
                               HorizontalAlignment="Center" Margin="0,8,0,0"
                               TextWrapping="Wrap"/>

                    <!-- Label size info -->
                    <Border Background="#E3F2FD" CornerRadius="4" Padding="10,8" Margin="0,16,0,0">
                        <StackPanel>
                            <TextBlock FontSize="11" Foreground="#1565C0" FontWeight="Bold"
                                       Text="Selected Label Size"/>
                            <TextBlock Text="{Binding SelectedLabelSize}"
                                       FontSize="13" Foreground="#0D47A1" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Stats -->
                    <Border Background="#F1F8E9" CornerRadius="4" Padding="10,8" Margin="0,8,0,0">
                        <StackPanel>
                            <TextBlock FontSize="11" Foreground="#388E3C" FontWeight="Bold"
                                       Text="Print Summary"/>
                            <TextBlock FontSize="12" Foreground="#2E7D32" Margin="0,4,0,0">
                                <Run Text="{Binding Products.Count, StringFormat='\{0\} products loaded'}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ── Bottom Toolbar ────────────────────────────────────────── -->
        <Border Grid.Row="2" Background="White" Margin="12,0,12,12"
                BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="16,10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="✓ Select All"
                            Style="{StaticResource SecondaryBtn}"
                            Command="{Binding SelectAllCommand}"
                            Margin="0,0,8,0"/>
                    <Button Content="🖨 Print Labels"
                            Style="{StaticResource PrimaryBtn}"
                            Command="{Binding PrintCommand}"
                            Margin="0,0,8,0"/>
                </StackPanel>

                <!-- Status message -->
                <TextBlock Text="{Binding StatusMessage}"
                           HorizontalAlignment="Right" VerticalAlignment="Center"
                           Foreground="#546E7A" FontSize="12"
                           Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibility}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
