<UserControl x:Class="Medital_Application.Views.Accounts.AccountListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="16,10" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="300" Padding="8,6" FontSize="13" BorderBrush="#B0BEC5" BorderThickness="1" Margin="0,0,8,0"/>
                <Button Content="Search" Command="{Binding SearchCommand}"
                        Background="#1565C0" Foreground="White" Padding="16,7" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="4">
            <DataGrid ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" BorderThickness="0"
                      RowBackground="White" AlternatingRowBackground="#F5F7FA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName}" Width="*"/>
                    <DataGridTextColumn Header="Group" Binding="{Binding GroupName}" Width="130"/>
                    <DataGridTextColumn Header="Mobile" Binding="{Binding Mobile}" Width="110"/>
                    <DataGridTextColumn Header="GSTIN" Binding="{Binding GSTIN}" Width="160"/>
                    <DataGridTextColumn Header="City" Binding="{Binding City}" Width="100"/>
                    <DataGridTextColumn Header="Balance" Binding="{Binding OpeningBalance, StringFormat='₹{0:N2}'}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
