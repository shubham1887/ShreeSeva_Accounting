<UserControl x:Class="Medital_Application.Views.Stock.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filter Bar -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="16,10" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="300" Padding="8,6" FontSize="13" BorderBrush="#B0BEC5" BorderThickness="1"
                         Margin="0,0,10,0"/>
                <Button Content="Search" Command="{Binding SearchCommand}"
                        Background="#1565C0" Foreground="White" Padding="16,7" BorderThickness="0" Cursor="Hand"
                        Margin="0,0,16,0"/>
                <Button Content="Export CSV" Command="{Binding ExportCsvCommand}"
                        Background="#2E7D32" Foreground="White" Padding="16,7" BorderThickness="0" Cursor="Hand"/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           FontSize="12" Foreground="#37474F" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Stock DataGrid -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="4">
            <DataGrid ItemsSource="{Binding StockItems}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" BorderThickness="0"
                      RowBackground="White" AlternatingRowBackground="#F5F7FA"
                      HeadersVisibility="Column" ColumnHeaderHeight="36">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRED">
                                <Setter Property="Background" Value="#FFEBEE"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRING_SOON">
                                <Setter Property="Background" Value="#FFF3E0"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                    <DataGridTextColumn Header="Manufacturer" Binding="{Binding ManufacturerName}" Width="150"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="120"/>
                    <DataGridTextColumn Header="Batch" Binding="{Binding BatchNo}" Width="90"/>
                    <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryMY}" Width="80"/>
                    <DataGridTextColumn Header="Qty" Binding="{Binding CurrentQty, StringFormat='N0'}" Width="70">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Rate" Binding="{Binding ActualRate, StringFormat='₹{0:N2}'}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="MRP" Binding="{Binding MRP, StringFormat='₹{0:N2}'}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Value" Binding="{Binding StockValue, StringFormat='₹{0:N0}'}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Status" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,2" HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#E8F5E9"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRED">
                                                    <Setter Property="Background" Value="#FFCDD2"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRING_SOON">
                                                    <Setter Property="Background" Value="#FFE0B2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding ExpiryStatus}" FontSize="10" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRED">
                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ExpiryStatus}" Value="EXPIRING_SOON">
                                                        <Setter Property="Foreground" Value="#E65100"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer stats -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="16,10" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding TotalItems, StringFormat='Total Items: {0}'}"
                           FontSize="13" FontWeight="SemiBold" Foreground="#37474F" Margin="0,0,24,0"/>
                <TextBlock Text="{Binding TotalValue, StringFormat='Stock Value: ₹{0:N0}'}"
                           FontSize="13" FontWeight="Bold" Foreground="#1565C0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
