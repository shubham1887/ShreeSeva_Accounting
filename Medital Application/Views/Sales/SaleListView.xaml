<UserControl x:Class="Medital_Application.Views.Sales.SaleListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter Bar -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="16,12" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="13"/>
                <DatePicker x:Name="DpFrom" FontSize="13" Margin="0,0,16,0"/>
                <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="13"/>
                <DatePicker x:Name="DpTo" FontSize="13" Margin="0,0,16,0"/>
                <Button Content="Load" Background="#1565C0" Foreground="White"
                        Padding="16,7" BorderThickness="0" Cursor="Hand"
                        Click="LoadButton_Click"/>
            </StackPanel>
        </Border>

        <!-- List -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="4">
            <DataGrid x:Name="SaleGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      GridLinesVisibility="Horizontal" BorderThickness="0"
                      RowBackground="White" AlternatingRowBackground="#F8FAFB"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Voucher No" Binding="{Binding VoucherNo}" Width="140"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding VoucherDate, StringFormat='dd-MMM-yyyy'}" Width="110"/>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="*"/>
                    <DataGridTextColumn Header="Patient" Binding="{Binding PatientName}" Width="140"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding NetAmount, StringFormat='₹{0:N2}'}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#1565C0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Mode" Binding="{Binding PaymentMode}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
